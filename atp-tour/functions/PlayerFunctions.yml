createPlayer:
  handler: src/handlers/player/createPlayer.handler
  events:
    - http:
        method: POST
        path: /player

getPlayer:
  handler: src/handlers/player/getPlayer.handler
  events:
    - http:
        method: GET
        path: /player/{id}